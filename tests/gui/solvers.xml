<?xml version="1.0" encoding="utf-8" ?>
<solvers xmlns="http://phys.p.lodz.pl/solvers.xsd">

  <solver name="Test" category="local" lib="something">
    Just a test solvers.xml file.

    <geometry type="Cartesian2D"/>

    <mesh type="Rectangular2D"/>

    <tag name="tag" label="Some tag">
      Configuration of the self-consistent loop.
      <attr name="attr1" label="Attribute one" type="float" default="0">First attribute.</attr>
      <attr name="attr2" label="Attribute two" type="choice">Second attribute.
        <choice>value1</choice>
        <choice>value2</choice>
      </attr>
    </tag>

    <flow>
      <receiver name="inReceiver"/>
      <provider name="outProvider"/>
    </flow>

  </solver>

</solvers>
