<?xml version="1.0" encoding="utf-8" ?>
<solvers xmlns="http://phys.p.lodz.pl/solvers.xsd">

  <solver name="Static2D" category="thermal" lib="fem">
    Two-dimensional static thermal solver in Cartesian geometry, based on finite-element method.

    <geometry type="Cartesian2D"/>

    <mesh type="Rectangular2D"/>

    <tag name="loop" label="Self-consistent loop">
      Configuration of the self-consistent loop.
      <attr name="inittemp" label="Initial temperature" type="float" unit="K">Initial temperature used for the first computation.</attr>
      <attr name="maxerr" label="Maximum allowed error" type="float" unit="K">Maximum allowed error in a self-consistent loop.</attr>
    </tag>

    <tag name="matrix" label="Matrix solver">
      Configuration of the matrix solver.
      <attr name="algorithm" label="Solution algorithm" type="choice">
        Algorithm used for solving set of linear positive-definite equations. Defaults to "&lt;i&gt;cholesky&lt;/i&gt;"
        <choice>cholesky</choice>
        <choice>gauss</choice>
        <choice>iterative</choice>
      </attr>
      <attr name="itererr" label="Max. residual error" type="float">Maximum allowed residual error for the iterative algorithm.</attr>
      <attr name="iterlim" label="Max. number of iterations" type="int">Maximum number of iterations for the iterative algorithm.</attr>
      <attr name="logfreq" label="Progress logging frequency" type="int">Number of iterations after which the progress is logged.</attr>
    </tag>

    <bcond name="temperature" label="Thermal"/>

    <bcond name="heatflux" label="Heat flux"/>

    <bcond name="convection" label="Convective">
      This boundary condition does not have &lt;i&gt;value&lt;/i&gt; attribute.
      Use &lt;i&gt;coeff&lt;/i&gt; for convection coefficient and &lt;i&gt;ambient&lt;/i&gt; for ambient temperature instead.
    </bcond>

    <bcond name="radiation" label="Radiative">
      This boundary condition does not have &lt;i&gt;value&lt;/i&gt; attribute.
      Use &lt;i&gt;emissivity&lt;/i&gt; for surface emissivity and &lt;i&gt;ambient&lt;/i&gt; for ambient temperature instead.
    </bcond>

    <flow>
      <receiver name="inHeat"/>
      <provider name="outTemperature"/>
    </flow>

  </solver>

  <solver name="StaticCyl" category="thermal" lib="fem">
    Two-dimensional static thermal solver in cylindrical geometry, based on finite-element method.

    <geometry type="Cylindrical"/>

    <mesh type="Rectangular2D"/>

    <tag name="loop" label="Self-consistent loop">
      Configuration of the self-consistent loop.
      <attr name="inittemp" label="Initial temperature" type="float" unit="K">Initial temperature used for the first computation.</attr>
      <attr name="maxerr" label="Maximum allowed error" type="float" unit="K">Maximum allowed error in a self-consistent loop.</attr>
    </tag>

    <tag name="matrix" label="Matrix solver">
      Configuration of the matrix solver.
      <attr name="algorithm" label="Solution algorithm" type="choice">
        Algorithm used for solving set of linear positive-definite equations.
        <choice>cholesky</choice>
        <choice>gauss</choice>
        <choice>iterative</choice>
      </attr>
      <attr name="itererr" label="Max. residual error" type="float">Maximum allowed residual error for the iterative algorithm.</attr>
      <attr name="iterlim" label="Max. number of iterations" type="int">Maximum number of iterations for the iterative algorithm.</attr>
      <attr name="logfreq" label="Progress logging frequency" type="int">Number of iterations after which the progress is logged.</attr>
    </tag>

    <bcond name="temperature" label="Temperature"/>

    <bcond name="heatflux" label="Heat flux"/>

    <bcond name="convection" label="Convective">
      This boundary condition does not have &lt;i&gt;value&lt;/i&gt; attribute.
      Use &lt;i&gt;coeff&lt;/i&gt; for convection coefficient and &lt;i&gt;ambient&lt;/i&gt; for ambient temperature instead.
    </bcond>

    <bcond name="radiation" label="Radiative">
      This boundary condition does not have &lt;i&gt;value&lt;/i&gt; attribute.
      Use &lt;i&gt;emissivity&lt;/i&gt; for surface emissivity and &lt;i&gt;ambient&lt;/i&gt; for ambient temperature instead.
    </bcond>

    <flow>
      <receiver name="inHeat"/>
      <provider name="outTemperature"/>
    </flow>

  </solver>
</solvers>
