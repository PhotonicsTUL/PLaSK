<?xml version="1.0" encoding="utf-8" ?>
<solvers xmlns="http://phys.p.lodz.pl/solvers.xsd">

  <solver name="Shockley2D" category="electrical" lib="shockley">
    Two-dimensional phenomenological solver in Cartesian geometry based Shockley equation and using finite-element method.

    <geometry type="Cartesian2D"/>

    <mesh type="Rectangular2D"/>

    <bcond name="voltage" label="Voltage"/>

    <tag name="loop" label="Configuration of the Self-Consistent Loop">
      Configuration of the self-consistent loop.
      <attr name="maxerr" label="Maximum current density error" type="float" unit="%" default="0.05">Maximum allowed current density error.</attr>
    </tag>

    <tag name="matrix" label="Matrix Solver">
      Configuration of the matrix solver.
      <attr name="algorithm" label="Solution algorithm" type="choice" default="cholesky">
        Algorithm used for solving set of linear positive-definite equations.
        <choice>cholesky</choice>
        <choice>gauss</choice>
        <choice>iterative</choice>
      </attr>
      <attr name="itererr" label="Max residual error" type="float" default="1e-8">Maximum allowed residual error for the iterative algorithm.</attr>
      <attr name="iterlim" label="Max number of iterations" type="int" default="10000">Maximum number of iterations for the iterative algorithm.</attr>
      <attr name="logfreq" label="Progress logging frequency" type="int" default="500">Number of iterations after which the progress is logged.</attr>
    </tag>

    <tag name="junction" label="Configuration of the Effective Model of P-N Junction">
      Configuration of the effective model of p-n junction.
      <attr name="beta#" label="Junction coefficients&lt;br/&gt;(one per line)" type="float" unit="1/V">Junction coefficients. This is an inverse of the junction thermal voltage.</attr>
      <attr name="js#" label="Reverse bias current densities&lt;br/&gt;(one per line)" type="float" unit="A/m&lt;sup&gt;2&lt;/sup&gt;">Reverse bias current densities.</attr>
      <attr name="pnjcond" label="Initial junctions conductivity" type="float" unit="S/m" default="5.0">Initial vertical conductivity of the junctions.</attr>
      <attr name="heat" label="Heat generation method" type="choice" default="joules">
        Method of determination of the heat generated in the junction.
        <choice>joules</choice>
        <choice>wavelength</choice>
      </attr>
      <attr name="wavelength" label="Emitted wavelength" type="float" unit="nm">Emitted wavelength if heat generation method is set to ‘&lt;i&gt;wavelength&lt;/i&gt;’.</attr>
    </tag>

    <tag name="contacts" label="Properties of the Contacts">
      Properties of the contact layers.
      <attr name="pcond" label="p-contact conductivity" type="float" unit="S/m" default="5.0">p-contact conductivity.</attr>
      <attr name="ncond" label="n-contact conductivity" type="float" unit="S/m" default="50.0">n-contact conductivity.</attr>
    </tag>

    <flow>
      <receiver name="inTemperature"/>
      <provider name="outCurrentDensity"/>
    </flow>
  </solver>

  <solver name="ShockleyCyl" category="electrical" lib="shockley">
    Two-dimensional phenomenological solver in cylindrical geometry based Shockley equation and using finite-element method.

    <geometry type="Cylindrical"/>

    <mesh type="Rectangular2D"/>

    <bcond name="voltage" label="Voltage"/>

    <tag name="loop" label="Configuration of the Self-Consistent Loop">
      Configuration of the self-consistent loop.
      <attr name="maxerr" label="Maximum current density error" type="float" unit="%" default="0.05">Maximum allowed current density error.</attr>
    </tag>

    <tag name="matrix" label="Matrix Solver">
      Configuration of the matrix solver.
      <attr name="algorithm" label="Solution algorithm" type="choice" default="cholesky">
        Algorithm used for solving set of linear positive-definite equations.
        <choice>cholesky</choice>
        <choice>gauss</choice>
        <choice>iterative</choice>
      </attr>
      <attr name="itererr" label="Max residual error" type="float" default="1e-8">Maximum allowed residual error for the iterative algorithm.</attr>
      <attr name="iterlim" label="Max number of iterations" type="int" default="10000">Maximum number of iterations for the iterative algorithm.</attr>
      <attr name="logfreq" label="Progress logging frequency" type="int" default="500">Number of iterations after which the progress is logged.</attr>
    </tag>

    <tag name="junction" label="Configuration of the Effective Model of P-N Junction">
      Configuration of the effective model of p-n junction.
      <attr name="beta#" label="Junction coefficients&lt;br/&gt;(one per line)" type="float" unit="1/V">Junction coefficients. This is an inverse of the junction thermal voltage.</attr>
      <attr name="js#" label="Reverse bias current densities&lt;br/&gt;(one per line)" type="float" unit="A/m&lt;sup&gt;2&lt;/sup&gt;">Reverse bias current densities.</attr>
      <attr name="pnjcond" label="Initial junctions conductivity" type="float" unit="S/m" default="5.0">Initial vertical conductivity of the junctions.</attr>
      <attr name="heat" label="Heat generation method" type="choice" default="joules">
        Method of determination of the heat generated in the junction.
        <choice>joules</choice>
        <choice>wavelength</choice>
      </attr>
      <attr name="wavelength" label="Emitted wavelength" type="float" unit="nm">Emitted wavelength if heat generation method is set to ‘&lt;i&gt;wavelength&lt;/i&gt;’.</attr>
    </tag>

    <tag name="contacts" label="Properties of the Contacts">
      Properties of the contact layers.
      <attr name="pcond" label="p-contact conductivity" type="float" unit="S/m" default="5.0">p-contact conductivity.</attr>
      <attr name="ncond" label="n-contact conductivity" type="float" unit="S/m" default="50.0">n-contact conductivity.</attr>
    </tag>

    <flow>
      <receiver name="inWavelength"/>
      <receiver name="inTemperature"/>
      <provider name="outVoltage"/>
      <provider name="outCurrentDensity"/>
      <provider name="outHeat"/>
      <provider name="outConductivity"/>
    </flow>
  </solver>

  <solver name="Shockley3D" category="electrical" lib="shockley">
    Three-dimensional phenomenological solver based Shockley equation and using finite-element method.

    <geometry type="Cartesian3D"/>

    <mesh type="Rectangular3D"/>

    <bcond name="voltage" label="Voltage"/>

    <tag name="loop" label="Configuration of the Self-Consistent Loop">
      Configuration of the self-consistent loop.
      <attr name="maxerr" label="Maximum current density error" type="float" unit="%" default="0.05">Maximum allowed current density error.</attr>
    </tag>

    <tag name="matrix" label="Matrix Solver">
      Configuration of the matrix solver.
      <attr name="algorithm" label="Solution algorithm" type="choice" default="cholesky">
        Algorithm used for solving set of linear positive-definite equations.
        <choice>cholesky</choice>
        <choice>gauss</choice>
        <choice>iterative</choice>
      </attr>
      <attr name="itererr" label="Max residual error" type="float" default="1e-8">Maximum allowed residual error for the iterative algorithm.</attr>
      <attr name="iterlim" label="Max number of iterations" type="int" default="10000">Maximum number of iterations for the iterative algorithm.</attr>
      <attr name="logfreq" label="Progress logging frequency" type="int" default="500">Number of iterations after which the progress is logged.</attr>
    </tag>

    <tag name="junction" label="Configuration of the Effective Model of P-N Junction">
      Configuration of the effective model of p-n junction.
      <attr name="beta" label="Junction coefficient" type="float" unit="1/V">Junction coefficient. This is an inverse of the junction thermal voltage.</attr>
      <attr name="js" label="Reverse bias current density" type="float" unit="A/m&lt;sup&gt;2&lt;/sup&gt;]&lt;br/&gt;">Reverse bias current density.</attr>
      <attr name="pnjcond" label="Initial junctions conductivity" type="float" unit="S/m" default="5.0">Initial vertical conductivity of the junctions.</attr>
      <attr name="heat" label="Heat generation method" type="choice" default="joules">
        Method of determination of the heat generated in the junction.
        <choice>joules</choice>
        <choice>wavelength</choice>
      </attr>
      <attr name="wavelength" label="Emitted wavelength" type="float" unit="nm">Emitted wavelength if heat generation method is set to ‘&lt;i&gt;wavelength&lt;/i&gt;’.</attr>
    </tag>

    <tag name="contacts" label="Properties of the Contacts">
      Properties of the contact layers.
      <attr name="pcond" label="p-contact conductivity" type="float" unit="S/m" default="5.0">p-contact conductivity.</attr>
      <attr name="ncond" label="n-contact conductivity" type="float" unit="S/m" default="50.0">n-contact conductivity.</attr>
    </tag>

    <flow>
      <receiver name="inWavelength"/>
      <receiver name="inTemperature"/>
      <provider name="outVoltage"/>
      <provider name="outCurrentDensity"/>
      <provider name="outHeat"/>
      <provider name="outConductivity"/>
    </flow>
  </solver>

</solvers>
