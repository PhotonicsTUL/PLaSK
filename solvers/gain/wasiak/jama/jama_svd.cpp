#include "jama_svd.h"

int minInt(int a, int b) // LUKASZ
{
    if(a < b)
        return a;
    return b;
}

int maxInt(int a, int b) // LUKASZ
{
    if( a < b )
        return b;
    return a;
}
